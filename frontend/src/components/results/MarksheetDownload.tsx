import { Download, FileText, Shield } from "lucide-react";
import { Button } from "@/components/ui/button";

interface MarksheetDownloadProps {
  semester: number;
  examType: string;
  onDownload: () => void;
}

export function MarksheetDownload({ semester, examType, onDownload }: MarksheetDownloadProps) {
  return (
    <div className="bg-card rounded-xl border border-border shadow-soft overflow-hidden">
      <div className="p-6">
        <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
          <div className="flex items-center gap-4">
            <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center">
              <FileText className="w-6 h-6 text-primary" />
            </div>
            <div>
              <h3 className="font-semibold text-foreground">Official Marksheet</h3>
              <p className="text-sm text-muted-foreground">
                Semester {semester} • {examType}
              </p>
            </div>
          </div>

          <Button onClick={onDownload} className="gap-2">
            <Download className="w-4 h-4" />
            Download PDF
          </Button>
        </div>

        <div className="mt-4 flex items-start gap-2 p-3 bg-muted/50 rounded-lg">
          <Shield className="w-4 h-4 text-muted-foreground mt-0.5 flex-shrink-0" />
          <p className="text-xs text-muted-foreground">
            This is an official document generated by the examination department.
          </p>
        </div>
      </div>
    </div>
  );
}
